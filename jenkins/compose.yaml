name: jenkins
services:
  app:
    build:
      dockerfile: app.Dockerfile
      args:
        # Get GID via "getent group docker" on host
        GID_DOCKER: 998
    volumes:
      - /home/destroflyer/jenkins/jenkins_home:/var/jenkins_home
      # Expose docker socket from host, so that builds can use it
      - /usr/bin/docker:/usr/bin/docker:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # Mount certificates directory, so that builds can access it via sh (outside of docker build stage)
      - /etc/letsencrypt:/etc/letsencrypt:ro
    ports:
      - "50000:50000"
    restart: always
  proxy:
    build:
      dockerfile: proxy.Dockerfile
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    ports:
      - "9999:443"
    restart: always